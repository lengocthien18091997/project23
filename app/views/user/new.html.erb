<!DOCTYPE html>
<html>
<head>
  <title>Đăng kí | Teacher Finder</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-..." crossorigin="anonymous">
</head>
<body class="new">
<div class="login-box">
  <h2>Teacher Finder</h2>

  <%= render "common/notice" %>

  <%= form_with url: register_path, method: :post do %>
    <label>Bạn là:</label>
    <div>
      <%= select_tag :role, options_for_select(
          [["", ""], ["Gia sư", "teacher"], ["Học viên", "student"]],
          selected: params[:role]
      ), class: "form-select" %>
    </div>

    <label>Email</label>
    <%= email_field_tag :new_email, nil, placeholder: "Nhập email" %>

    <label>Mật khẩu</label>
    <%= password_field_tag :new_password, nil, placeholder: "Nhập mật khẩu" %>

    <label>Nhập lại mật khẩu</label>
    <%= password_field_tag :re_password, nil, placeholder: "Nhập lại mật khẩu" %>

    <button type="submit">Đăng kí</button>
    <div class="d-flex justify-content-center"><%= link_to "Quay lại đăng nhập", login_path, class: "btn btn-link" %></div>
  <% end %>
</div>
</body>
</html>
